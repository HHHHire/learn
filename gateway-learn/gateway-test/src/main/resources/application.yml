server:
  port: 9400
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

spring:
  application:
    name: gateway-server
  profiles:
    active: @profile.active@
